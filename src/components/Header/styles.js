import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  header: {
    paddingLeft: 20,
  },
  headerText: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#6284DA',
  },
});

export default styles;
